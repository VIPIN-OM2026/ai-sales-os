[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "./node_modules/.bin/prisma generate",
  "./node_modules/.bin/prisma migrate deploy",
  "npm run build"
]

[start]
cmd = "node dist/main"
